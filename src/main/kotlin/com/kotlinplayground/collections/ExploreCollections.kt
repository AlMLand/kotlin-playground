package com.kotlinplayground.collections

private val map = mapOf('a' to "a", 'b' to "b")
private val hashMap = hashMapOf('a' to "a", 'b' to "b")

private val set = setOf("a", "b", "b")

fun main() {
    println(set)
}
